import React from 'react';

const Loading = () => {
	return (
		<div className="h-[80vh] w-screen flex items-center justify-center">
			<div className="  text-2xl font-semibold animate-bounce">Loading...</div>
		</div>
	);
};

export default Loading;
